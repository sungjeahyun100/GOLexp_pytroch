# GPU 버전 Python 의존성
# PyTorch with CUDA support (GPU version)
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Core dependencies
numpy>=1.21.0
matplotlib>=3.5.0
pygame>=2.5.0

# Optional but recommended
scipy>=1.9.0
pillow>=9.0.0
tqdm>=4.64.0