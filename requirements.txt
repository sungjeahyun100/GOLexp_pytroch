# GPU 버전 Python 의존성
# PyTorch with CUDA support (GPU version)
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Core dependencies
numpy>=1.21.0
matplotlib>=3.5.0

# Optional but recommended
scipy>=1.7.0
pillow>=8.3.0
tqdm>=4.64.0
argparse
Pillow>=9.0.0
scipy>=1.9.0